version: "3"

services:
  api:
    build: api
    ports:
      - "3000:3000"
    depends_on:
      - database
  database:
    build: database
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data